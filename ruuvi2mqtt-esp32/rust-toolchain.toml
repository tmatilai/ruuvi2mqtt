[toolchain]
# RISC-V targets (ESP32-C6, ESP32-C3) need nightly for build-std support.
# Xtensa targets (ESP32, ESP32-S3) require the Espressif fork: cargo +esp
# Install with: cargo install espup && espup install
channel = "nightly"
components = ["rust-src"]
